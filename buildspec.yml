version: 0.2
phases:
  build:
    commands:
      - echo Building...
artifacts:
  files:
    - "**/*"
  secondary-artifacts:
    artifact1:
      files:
        - directory/file1
      name: secondary-artifact-name-1